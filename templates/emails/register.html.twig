<h1>Activé votre compte</h1>
<p>Bonjour {{ user.firstname }}</p>
<p>Votre inscription sur le site e-commerce doit être validé en cliquant sur le lien ci-dessous : </p>
<p><a href="{{ absolute_url(path('verify_user', {token: token}))}}">{{ absolute_url(path('verify_user', {token: token}))}}</a></p>
<p>Ce lien expirera dans 3h</p>